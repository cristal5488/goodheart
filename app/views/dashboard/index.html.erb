<!-- if is a donor -->
<% if current_donor %>
  <%= render partial: 'donors/profile' %>
  <!-- render a partil for donor -->
<% else %>
  <%= render partial: 'health_providers/profile'%>
<% end %>


<%= render 'events/index', events: @events %>
